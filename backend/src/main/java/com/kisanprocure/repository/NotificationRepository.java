package com.kisanprocure.repository; import com.kisanprocure.entity.Notification; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface NotificationRepository extends JpaRepository<Notification,Long>{ List<Notification> findByUserIdOrderByIdDesc(Long userId); Optional<Notification> findByIdAndUserId(Long id,Long userId); }